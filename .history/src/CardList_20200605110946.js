import React from 'react';
import Card from './Card';

const CardList = ({ robots }) => {
    const cardsArray = 
    return (
        <div>
            {cardsArray}
        </div>
    );
}

export default CardList;